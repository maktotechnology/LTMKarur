@{
    Layout = "_Layout";
    ViewData["Title"] = "Dashboard";
}

<div class="container mt-5">
    <h3>Welcome, @User.Identity?.Name!</h3>

    <div class="row mt-4">
        <div class="col-md-4">
            <div class="card shadow-sm p-3 mb-3">
                <h5>Sales Dashboard</h5>
                <p class="text-muted">Summary of sales performance</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm p-3 mb-3">
                <h5>Purchases</h5>
                <p class="text-muted">Manage purchase orders</p>
            </div>
        </div>
    </div>
</div>
